apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ledgerly-certificate
spec:
  secretName: ledgerly-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - ledgerly.colddev.net
