apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: ledgerly-db-backup
  namespace: ledgerly
spec:
  backupOwnerReference: cluster
  cluster:
    name: ledgerly-db-production-cluster-v1
  immediate: true
  schedule: "0 0 2 * *"
